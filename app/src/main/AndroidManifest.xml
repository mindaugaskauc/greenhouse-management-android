<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.crop_management_system">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Crop_Management_System">
        <activity android:name=".MainLoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SendEmailActivity" />
        <activity android:name=".w_temp" />
        <activity android:name=".s_temp" />
        <activity android:name=".hum" />
        <activity android:name=".s_ph" />
      <!--  <activity android:name=".modes" /> -->
        <activity android:name=".stat" />
        <activity
            android:name=".NavigationActivity"
            android:label="@string/title_activity_navigation"
            android:theme="@style/Theme.Crop_Management_System.NoActionBar" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".RegisterActivity" />
    </application>

</manifest>